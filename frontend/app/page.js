export default function Home() {
  return (
    <main style={{ padding: '2rem' }}>
      <h1>Disaster Alert</h1>
      <p>PWA: Install for offline access. Trainings, lessons, risk layers, and announcements are cached when online.</p>
    </main>
  );
}
